version: "2"

services:
  skola24asical.se:
    build: .
    container_name: skola24asical.se
    restart: unless-stopped
    ports:
      - 8080:8080
#    volumes:
#      - "./conf.d/:/etc/nginx/conf.d"
    environment:
      - VIRTUAL_HOST=skola24asical.se
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=skola24asical.se
      - LETSENCRYPT_EMAIL=robingronberg@gmail.com
    entrypoint: node app.js